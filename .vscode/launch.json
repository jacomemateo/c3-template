{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug C3",
            // This points to the binary produced by your 'debug' target
            "program": "${workspaceFolder}/bin/debug", 
            "args": [],
            "cwd": "${workspaceFolder}",
            // This ensures the code is built before the debugger starts
            "preLaunchTask": "C3: Run Debug" 
        }
    ]
}