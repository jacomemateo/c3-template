{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug C3",
            "program": "${workspaceFolder}/bin/debug", 
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "C3: Build Debug" // <--- MUST MATCH THE TASK LABEL
        }
    ]
}